# =============================================================================
# HedgeDoc Configuration
# =============================================================================
# Copy this to .env before first run (or run `make up` which does it for you).
# Lines marked <auto-generated> are filled in by scripts/setup.sh on first run.

# -- Domain & Access ----------------------------------------------------------
# Set to your domain for production (e.g., notes.example.com).
# For local dev, localhost works fine.
HEDGEDOC_DOMAIN=localhost
HEDGEDOC_PORT=80

# -- Security -----------------------------------------------------------------
# Session secret signs cookies. Must be random and persistent across restarts.
# Auto-generated on first run — do not commit this value.
CMD_SESSION_SECRET=<auto-generated>

# -- Database -----------------------------------------------------------------
# Internal to Docker network — no need to change unless you bring your own DB.
POSTGRES_USER=hedgedoc
POSTGRES_PASSWORD=<auto-generated>
POSTGRES_DB=hedgedoc

# -- HedgeDoc Features --------------------------------------------------------
# Allow anonymous users to view and create notes (good for local/team use).
CMD_ALLOW_ANONYMOUS=true
CMD_ALLOW_ANONYMOUS_EDITS=true

# Allow creating notes by visiting any URL (Etherpad-style).
CMD_ALLOW_FREEURL=true

# Default permission for new notes (editable, freely, limited, locked, private).
CMD_DEFAULT_PERMISSION=editable

# Enable the /status and /metrics endpoints for monitoring.
CMD_ENABLE_STATS_API=true

# -- Email Auth ---------------------------------------------------------------
# Local email/password accounts are enabled by default.
CMD_EMAIL=true
CMD_ALLOW_EMAIL_REGISTER=true

# -- Image Uploads ------------------------------------------------------------
# Store uploaded images on the local filesystem (inside the container).
CMD_IMAGE_UPLOAD_TYPE=filesystem

# -- Advanced -----------------------------------------------------------------
# Max document length in characters (default 100,000).
# CMD_DOCUMENT_MAX_LENGTH=100000

# Uncomment to enable GitHub OAuth login:
# CMD_GITHUB_CLIENTID=your-client-id
# CMD_GITHUB_CLIENTSECRET=your-client-secret
